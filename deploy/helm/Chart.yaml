apiVersion: v2
name: gpusprint
description: A Helm chart for deploying the gpusprint GPU metrics exporter
type: application
# version and appVersion are set dynamically by CI during release.
# For local development, these placeholders are used.
version: 0.0.0-dev
appVersion: "latest"
